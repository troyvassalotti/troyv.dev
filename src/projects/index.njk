---
layout: base
title: Projects
---

<h1 class="u-revertMargin--start">{{ title }}</h1>
<ul role="list">
  {% for project in projects | reverse %}
    <li>
      <article>
        <h2>{{ project.name }}</h2>
        <ul>
          {% if project.url %}
            <li>See the live site: <a href="{{ project.url }}">{{ project.url }}</a></li>
          {% endif %}
          {% if project.repository %}
            <li>
              <a class="c-link--icon" href="{{ project.repository }}">{% include 'partials/github.svg' %}{{ project.repository | replace("https://github.com/", "") }}</a>
            </li>
          {% endif %}
        </ul>
      </article>
    </li>
  {% endfor %}
</ul>
