---
title: Welcome
description: Watch as Troy Vassalotti learns his way around a computer.
---

{% extends "layouts/base.njk" %}
{% from "macros.njk" import postItem %}

{% block content %}
    <main id="main">
        <cloudy-sky hillside>
        <div class="wrapper flow constrain--some">
            <div class="container--read flow">
                <type-writer class="typewriter--read">
                <h2 id="read" class="typing--read">Read</h2>
                </type-writer>
                <ol role="list" class="c-postList flow">
                    {% for post in collections.post.slice(collections.post.length - 5, collections.post.length) | reverse %}
                        <p class="u-step-2 u-text--bold">{{ post.data.description }}</p>
                        <p class="u-step--1">
                            From the post <a class="u-text--italic" href="{{ post.url }}">{{ post.data.title }}</a>, published {{ post.date | dateString }}.
                        </p>
                    {% endfor %}
                </ol>
            </div>
            <div class="container--play flow">
                <type-writer>
                <h2 id="play" class="typing--play">Play</h2>
                </type-writer>
                <ol role="list" class="list play u-grid" data-grid-columns="fluid">
                    {% for project in projects %}
                        {% if project.tags and project.tags | inArray("play") %}
                            <li class="play__item">
                                <article class="flow play__item-content">
                                    <p class="play__item-title u-step-2 u-text--bold">
                                        <a href="{{ project.url }}">{{ project.name }}</a>
                                    </p>
                                    <p class="play__item-description">{{ project.description }}</p>
                                </article>
                            </li>
                        {% endif %}
                    {% endfor %}
                </ol>
            </div>
            <div class="container--listen flow">
                <type-writer>
                <h2 id="listen" class="typing--listen">Listen</h2>
                </type-writer>
                <!-- htmlmin:ignore -->
                <p>
                    I'm currently listening to <now-playing service="https://api.troyv.dev/now-playing">
                    <span>elevator music</span>
                    </now-playing> but you can listen to other songs below.
                </p>
                <!-- htmlmin:ignore -->
                <plvy-list class="plvylist" file="/assets/js/plvylist.json">
                </plvy-list>
                <p class="u-text--italic u-step--1">Psst... I made <a href="https://plvylist.troyv.dev">this music player</a>.</p>
            </div>
        </div>
        </cloudy-sky>
    </main>
{% endblock content %}

{% block style %}
    <style>
        :is(.container--read, .container--play) {
            margin-block-end: var(--space-3xl);
        }

        :is(.typing--read, .typing--play, .typing--listen) {
            --kb-duration-typing: .75s;
            font-size: var(--step-6);
        }

        .typewriter--read {
            display: grid;
            padding-block-start: var(--space-s-m);
            place-content: end;
        }

        .typing--read {
            --kb-steps-typing: 15;
        }

        .typing--play {
            --kb-steps-typing: 15;
        }

        .typing--listen {
            --kb-steps-typing: 15;
        }

        .container--read {
            text-align: end;
        }

        .list.play {
            --fluid-col-size: 20ch;
        }

        .play__item-content {
            border-radius: var(--space-3xs);
            box-shadow: 1px 2px 1px 2px var(--foreground);
            padding-block: var(--space-s);
            padding-inline: var(--space-xs);
        }

        .plvylist {
            color-scheme: light dark;
            font-size: var(--step--1);
            max-inline-size: 70rem;
        }

        ::part(hillside) {
            fill: var(--accent);
        }

        @media(prefers-color-scheme: light) {
            cloudy-sky {
                --cloud-filter: hue-rotate(222deg);
            }
        }
    </style>
{% endblock style %}

{% block script %}
    {{ super() }}
    <script type="module">
        import {CloudySky} from "cloudysky"
        import {Typewriter} from "typewriter"
        import "/assets/js/now-playing.js";
        import Plvylist from "plvylist";
    </script>
{% endblock script %}
