{% extends 'layouts/base.njk' %}

{% block content %}
  <main id="main">
    <div class="wrapper constrain--less">
      <header class="o-section--angled wrapper constrain--base">
        <div class="container u-text--center u-truncate flow">
          <h1>{{ title }}</h1>
          <p>I write for a few projects, so here they are with all the important links. Music is best managed elsewhere than my personal
            website, so you're better off visiting those places.</p>
        </div>
      </header>
      <div class="l-page">
        <section class="content">
          <article class="band">
            <div class="logo">
              <a href="https://www.frontroyalband.com/">
                <img
                  src="{{ metadata.cloudinary }}/c_scale,f_auto,q_auto:eco,w_400/v1646349103/blog/front-royal-logo-white_400x240_fharjl.webp"
                  alt="Front Royal"
                  width="400"
                  height="240"
                  loading="lazy"
                  decoding="async" />
              </a>
            </div>
            <div class="links">
              {% for social in frontRoyalSocial %}
                <a href="{{ social.href }}">
                  {% include "partials/" + social.img %}
                </a>
              {% endfor %}
            </div>
          </article>
          <article class="band">
            <div class="logo">
              <a href="https://hyperfollow.com/troyalllowercase">
                <img
                  src="{{ metadata.cloudinary }}/c_scale,f_auto,q_auto:eco,w_300/v1646349104/blog/troy-full_jx1oq4.webp"
                  alt="troy"
                  width="300"
                  height="150"
                  loading="lazy"
                  decoding="async" />
              </a>
            </div>
            <div class="links">
              {% for social in troySocial %}
                <a href="{{ social.href }}">
                  {% include "partials/" + social.img %}
                </a>
              {% endfor %}
            </div>
          </article>
        </section>
        <section class="plvylist">
          <h2>You Can Stream Here</h2>
          <p>I <a href="/posts/plvylist-web-component">created my own</a> embedded streaming player, called <a
              href="https://github.com/troyvassalotti/plvylist">Plvylist</a>.
            Check it out!</p>
          <plvylist-player tracks="/assets/js/alone-in-a-crowded-room.json" starting-volume="0.75">
          </plvylist-player>
        </section>
      </div>
    </div>
  </main>
{% endblock %}

{% block style %}
  {{ super() }}
  <style>
    header {
      --wrapper-padding: 1rem;
    }

    .l-page {
      align-items: baseline;
      display: grid;
      gap: var(--space-l);
      grid-template-columns: 1fr 1fr;
    }

    .container p {
      margin-inline: auto;
    }

    img, svg {
      display: inline-block;
    }

    .links {
      display: flex;
      gap: var(--space-2xs);
    }

    .links svg {
      max-inline-size: 48px;
    }

    .plvylist {
      inset-block-start: 0;
      position: sticky;
    }
  </style>
{% endblock %}
