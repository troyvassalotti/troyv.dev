---
layout: base
title: Music Collection
description: A place to keep track of my physical music collection.
---
<h1>{{ title }}</h1>
<p>{{ description }}</p>
<article id="vinyl" class="vinyl flow">
    <header>
        <h2>Vinyl</h2>
    </header>
    <section class="twelveInch flow">
        <h3>12 Inch</h3>
        <ul>
            {% for item in vinyl["12 Inch"] %}
                <li>
                    <b>{{ item.artist }}</b>:
                    <i>{{ item.album }}</i>
                </li>
            {% endfor %}
        </ul>
    </section>
    <section class="tenInch flow">
        <h3>10 Inch</h3>
        <ul>
            {% for item in vinyl["10 Inch"] %}
                <li>
                    <b>{{ item.artist }}</b>:
                    <i>{{ item.album }}</i>
                </li>
            {% endfor %}
        </ul>
    </section>
    <section class="sevenInch flow">
        <h3>7 Inch</h3>
        <ul>
            {% for item in vinyl["7 Inch"] %}
                <li>
                    <b>{{ item.artist }}</b>:
                    <i>{{ item.album }}</i>
                </li>
            {% endfor %}
        </ul>
    </section>
    <section class="wants flow">
        <h3>The Want List</h3>
        <p>I <em>could</em> order these online but the point of this list is that I find them out in the wild. I don't even know if all of these have been pressed before, but I'm holding out hope.</p>
        <ul>
            {% for item in vinyl["Wants"] %}
                <li>
                    <b>{{ item.artist }}</b>:
                    <i>{{ item.album }}</i>
                </li>
            {% endfor %}
        </ul>
    </section>
</article>
