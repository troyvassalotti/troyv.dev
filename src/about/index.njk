---
title: About
description: Normal person stuck in a computer.
---

{% extends 'layouts/base.njk' %}

{% block content %}
  <div class="wrapper constrain--more">
    <main id="main" class="flow">
      <header>
          <h1>{{ title }}</h1>
      </header>
      <section class="aboutMe flow u-truncate">
        {% renderFile './src/_includes/partials/bio.md' %}
        <h2>Where to Find Me</h2>
        <p>My social media presence is sparse, but I am found in a few spots:</p>
        <ul class="c-socialMedia" role="list">
          {% for social in metadata.socials %}
            <li><a href="{{ social.url }}">{% include 'partials/' + social.name | lower + '.svg' %}{{ social.name }}</a></li>
          {% endfor %}
        </ul>
      </section>
    </main>
  </div>
{% endblock %}

{% block style %}
  {{ super() }}
  <style>
    .quickLinks ol {
      padding: 0;
    }

    .c-socialMedia a {
      align-items: center;
      display: inline-flex;
      gap: .5em;
    }

    .c-socialMedia a:not(:first-of-type) {
      margin-block-start: 1ch;
    }

    .c-socialMedia svg {
      display: inline-block;
      max-inline-size: 28px;
    }
  </style>
{% endblock %}
