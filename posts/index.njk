---
title: Posts
description: I occasionally write about things that enter my mind or the excessive amounts of data I keep on myself.
layout: primary
pageName: postsPage
addCSS: true
addJS: true
---
<main class="layout">
  <header class="page-header center">
    <div class="wrapper">
      <h1><span id="posts-title">{{ title }}</span></h1>
    </div>
  </header>
  <section class="all-posts full">
    <div class="wrapper">
      <div class="grid posts-container">
      {%- for post in collections.post | reverse -%}
        <article class="post-listing {{ 'featured-image' if post.data.featuredImage  }}" {% if post.data.featuredImage %} style="--featuredImage: {{ post.data.featuredImage }}" {% endif %}>
          <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          <p>{{ post.data.description }}</p>
        </article>
      {%- endfor -%}
      </div>
    </div>
  </section>
</main>
